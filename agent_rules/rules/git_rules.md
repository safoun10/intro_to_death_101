# VERSION CONTROL POLICY
- You are strictly FORBIDDEN from executing any Git commands (e.g., git commit, git push, git worktree, git checkout, git rebase).
- You must only operate within the currently active branch workspace.
- If you notice the current branch is 'main', stop immediately and ask the user to switch to 'dev'.
- File staging, committing, and deployment are the sole responsibility of the user.
