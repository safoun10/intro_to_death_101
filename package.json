{
  "name": "death-101",
  "version": "1.0.0",
  "description": "Visual blog on mortality",
  "main": "src/js/main.js",
  "scripts": {
    "build:content": "node scripts/build-content.mjs",
    "build:dist": "node scripts/build-dist.mjs",
    "build": "npm run build:content && npm run build:dist",
    "dev": "node scripts/build-content.mjs && npx serve ."
  },
  "devDependencies": {
    "serve": "^14.2.1"
  }
}